# pip でインストールするもの
mistune>=3.0
Pillow
watchfiles
wcwidth
Pygments
cairosvg        # 数式(SVG)→PNG ラスタライズ用

# pycairo / PyGObject はディストリのパッケージから入れること（README参照）
#   Arch:   sudo pacman -S python-cairo python-gobject
#   Debian: sudo apt install python3-gi python3-gi-cairo gir1.2-pango-1.0
# pip で入れる場合は system の girepository / cairo 開発ヘッダが必要:
#   pycairo
#   PyGObject
